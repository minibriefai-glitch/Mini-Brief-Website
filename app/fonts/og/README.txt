Static Inter weights (from the same official release as ../InterVariable.woff2,
SIL OFL 1.1, see ../LICENSE.txt) used only to render app/opengraph-image.tsx at
build time. Satori cannot read WOFF2, so these TTFs exist beside it.
